///////////////////////////////////////////////////////////////////////////////
// Code generated by wxUiEditor - see https://github.com/KeyWorksRW/wxUiEditor/
//
// Do not edit any code above the "End of generated code" comment block.
// Any changes before that block will be lost if it is re-generated!
///////////////////////////////////////////////////////////////////////////////

// clang-format off

#include <wx/artprov.h>
#include <wx/bitmap.h>
#include <wx/icon.h>
#include <wx/image.h>

#include "mdi_frame.h"

#include "mainapp.h"

bool MdiFrame::Create(wxDocManager* manager, wxFrame* parent, wxWindowID id, const wxString& title,
    const wxPoint& pos, const wxSize& size, long style, const wxString &name)
{

    if (!wxDocMDIParentFrame::Create(manager, parent, id, title, pos, size, style, name))
    {
        return false;
    }
    if (pos != wxDefaultPosition || size != wxDefaultSize)
    {
        SetSize(FromDIP(pos).x, FromDIP(pos).y,
        FromDIP(size).x, FromDIP(size).y, wxSIZE_USE_EXISTING);
    }

    m_toolBar = new wxToolBar(this, wxID_ANY, wxDefaultPosition, wxDefaultSize, wxTB_HORIZONTAL);
    m_toolBar->Realize();

    m_menubar = new wxMenuBar();

    m_menu = new wxMenu();
    auto* menu_item = new wxMenuItem(m_menu, wxID_NEW);
    menu_item->SetBitmap(wxArtProvider::GetBitmapBundle(wxART_NEW, wxART_MENU));

    m_menu->Append(menu_item);
    auto* menu_item2 = new wxMenuItem(m_menu, wxID_OPEN);
    menu_item2->SetBitmap(wxArtProvider::GetBitmapBundle(wxART_FILE_OPEN, wxART_MENU));

    m_menu->Append(menu_item2);
    m_menu->AppendSeparator();
    auto* menu_item3 = new wxMenuItem(m_menu, wxID_EXIT);
    menu_item3->SetBitmap(wxArtProvider::GetBitmapBundle(wxART_QUIT, wxART_MENU));

    m_menu->Append(menu_item3);
    m_menubar->Append(m_menu, wxGetStockLabel(wxID_FILE));

    m_menu2 = new wxMenu();
    m_menubar->Append(m_menu2, wxGetStockLabel(wxID_EDIT));

    m_menu3 = new wxMenu();
    auto* menu_item4 = new wxMenuItem(m_menu3, wxID_ABOUT);
    m_menu3->Append(menu_item4);
    m_menubar->Append(m_menu3, wxGetStockLabel(wxID_HELP));

    SetMenuBar(m_menubar);

    m_statusBar = CreateStatusBar();

    Centre(wxBOTH);

    // Event handlers
    Bind(wxEVT_MENU,
        [](wxCommandEvent& event)
        {
            wxDynamicCast(wxApp::GetInstance(), MainApp)->OnAbout(event);
        },
        wxID_ABOUT);

// Unimplemented Event handler functions
// Copy any of the following and paste them below the comment block, or to your inherited class.

/*

*/

    return true;
}

// ************* End of generated code ***********
// DO NOT EDIT THIS COMMENT BLOCK!
//
// Code below this comment block will be preserved
// if the code for this class is re-generated.
//
// clang-format on
// ***********************************************
